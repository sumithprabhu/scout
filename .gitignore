# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# env — never commit real secrets
.env
.env.local
.env*.local

# bright data local artifacts
/scrapers/**/dist
*.log

# typescript
*.tsbuildinfo

# local working files (contain API keys in logs) — never commit
/scratchpad/

# reference template dropped in locally (third-party branding) — not part of the app
/semrush/

# any nested deps/build artifacts
**/node_modules/
**/.next/
